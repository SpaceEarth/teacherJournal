import { MarkColorDirective } from './mark-color.directive';

describe('MarkColorDirective', () => {
  it('should create an instance', () => {
    const directive: MarkColorDirective = new MarkColorDirective();
    expect(directive).toBeTruthy();
  });
});
