import { TableSortPipe } from './table-sort.pipe';

describe('TableSortPipe', () => {
  it('create an instance', () => {
    const pipe: TableSortPipe = new TableSortPipe();
    expect(pipe).toBeTruthy();
  });
});
